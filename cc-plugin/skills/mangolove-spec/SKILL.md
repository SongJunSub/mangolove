---
name: mangolove-spec
description: "MangoLove Medium/Large 트랙에서 Spec 을 작성·검토할 때 사용한다. 7종 Spec 템플릿(API · 리팩토링 · 인프라/CICD · 배치 · 버그 · UI), Spec 적대적 리뷰 체크리스트, Product/Engineering 리뷰, 단일 최종 승인 제시 형식을 제공한다. 새 기능·API 수정·리팩토링·스키마 변경 등 Spec 이 필요한 작업에서 호출한다."
---

# MangoLove — Spec 작성 & 사전 리뷰 (mangolove-spec)

이 스킬은 strict 방법론 Large 워크플로우의 **분석 → Spec → Spec 적대적 리뷰 → Product/Engineering 리뷰 → 최종 승인** 단계 상세다.
트랙 판정 · 승인 원칙 · 안전 절차(dry-run · 메모리 · 경계면)는 **코어(core.md)** 에 있으며 그것이 authoritative 다.
Spec 은 세션 대화(메모리)에만 유지하고 레포에 파일로 남기지 않는다.

### 1단계: 분석 (항상 먼저)
사용자가 문제나 기능을 설명하면:
1. 응답 전에 관련 파일을 모두 읽을 것
2. 전체 호출 체인을 추적 (Controller -> Service -> Repository -> Entity -> DTO)
3. 영향 받는 모든 파일을 식별
4. **Medium/Large Track에서는 `/plan` 스킬을 호출**하여 구조화된 계획 모드로 진입한다
   - Plan 모드에서 분석 결과와 구현 계획을 체계적으로 정리
   - 계획이 확정되면 Plan 모드를 종료하고 다음 단계로 진행
5. 분석 결과를 자체적으로 확정 (사용자에게 중간 확인 X)
6. 분석은 이후 단계(Spec 작성)의 입력이 되며, 최종 승인 단계에서 통합 제시한다

### 2단계: Spec 작성 (필수)
분석이 완료되면 구현 전에 반드시 Spec을 작성한다.
변경 유형에 따라 적절한 템플릿을 선택한다. TBD, "추후 결정", 미확정 항목이 하나라도 있으면 이 단계를 통과할 수 없다.

#### Spec 보관 방식
- Spec은 **세션 메모리(대화 컨텍스트)에만 유지**한다. 레포지토리에 `.mangolove/specs/*.md` 같은 파일을 생성하지 않는다.
- 리뷰 서브에이전트에게 Spec을 전달할 때는 파일 경로 대신 프롬프트에 **인라인으로 포함**시킨다.
- 세션이 끊기면 Spec을 다시 작성하거나 Linear 티켓 description에 필요한 만큼 옮겨 적는다 (레포 오염 방지).
- Trivial/Small Track은 Spec을 작성하지 않는다.

#### API 변경 (신규/수정) 템플릿
```
📝 Spec: [feature name]

## API Contract
- Endpoint: [METHOD /path]
- Request Body: [JSON schema or fields]
- Response Body: [JSON schema or fields]
- Error Responses: [status codes and messages]

## Data Model
- Entity/Table 변경 사항
- 새 필드, 타입, 제약조건, 인덱스

## 동작 명세
- 정상 흐름 (Happy Path)
- 엣지 케이스별 예상 동작
- 에러 시나리오별 처리 방식

## 수용 기준
- [ ] 기준 1
- [ ] 기준 2
```

#### 리팩토링 템플릿
```
📝 Spec: [리팩토링 대상]

## Before/After 구조 비교
- Before: [현재 구조 설명]
- After: [변경 후 구조 설명]
- 변경 동기: [왜 구조를 바꾸는가]

## 변경 범위
- 수정 파일 목록
- 삭제/이동되는 클래스/메서드

## 마이그레이션 전략 (한 PR로 들어가지 않는 큰 변경일 때 필수)
- 선택: [Compatibility Layer / Module-by-Module Port / Branch by Abstraction / 해당 없음]
- 선택 근거: [왜 이 전략인가 — "해당 없음"이면 한 PR로 충분한 이유]
- 단계별 롤백 지점: [있음/없음 — 없으면 전략 재선택]

## 동작 동일성 증명
- 기존 테스트 전부 통과 (추가/수정 없이)
- 외부 API 계약 변경 없음
- DB 스키마 변경 없음

## 수용 기준
- [ ] 기존 테스트 100% 통과
- [ ] 외부 인터페이스 변경 없음
```

#### 인프라/CI/CD 변경 템플릿
```
📝 Spec: [변경 대상]

## 변경 대상
- 파일: [워크플로우/설정 파일 경로]
- 변경 내용: [구체적 변경 사항]

## 환경별 영향
- dev: [영향 여부]
- staging: [영향 여부]
- prod: [영향 여부]

## 롤백 절차
- [롤백 방법과 순서]

## 시크릿/권한 변경
- 새 시크릿: [있으면 명시]
- 권한 변경: [있으면 명시]

## 수용 기준
- [ ] 기준 1
- [ ] 기준 2
```

#### 배치/스케줄러 템플릿
```
📝 Spec: [배치 작업명]

## 입력/출력
- 입력 데이터: [소스, 형식, 예상 건수]
- 출력 데이터: [대상, 형식]

## 실행 조건
- 트리거: [cron 표현식 / 이벤트 / 수동]
- 실행 주기: [매일/매시간/등]

## 처리 전략
- 대량 데이터 처리: [배치 크기, 페이지네이션]
- 실패 시 재시도: [최대 횟수, 백오프 전략]
- 부분 실패 처리: [건너뛰기 / 중단 / 보상 트랜잭션]

## 수용 기준
- [ ] 기준 1
- [ ] 기준 2
```

#### 버그 수정 템플릿
```
📝 Spec: [버그 제목]

## 재현 조건
- 환경: [dev/staging/prod]
- 재현 단계: [1. 2. 3.]
- 예상 동작: [정상적으로 어떻게 되어야 하는가]
- 실제 동작: [현재 어떻게 되는가]

## 근본 원인
- 원인: [코드 레벨에서의 원인]
- 영향 범위: [이 버그로 영향받는 다른 기능]

## 수정 방향
- 수정할 파일과 변경 내용
- 사이드 이펙트 유무

## 수용 기준
- [ ] 버그 재현 시나리오에서 정상 동작 확인
- [ ] 회귀 테스트 추가
```

#### UI 변경 템플릿
```
📝 Spec: [UI 변경 대상]

## 화면 상태별 동작
- 초기 상태: [로딩 전]
- 로딩 중: [스피너/스켈레톤]
- 정상 표시: [데이터 있을 때]
- 빈 상태: [데이터 없을 때]
- 에러 상태: [API 실패 시]

## 인터랙션
- 사용자 액션: [클릭/입력/스크롤 등]
- 예상 반응: [각 액션의 결과]

## 반응형/접근성
- 반응형 대응: [모바일/태블릿/데스크톱]
- 접근성: [키보드 내비게이션, 스크린 리더]

## 수용 기준
- [ ] 기준 1
- [ ] 기준 2
```

### 3단계: Spec 리뷰 — 적대적 검증
Spec 작성 완료 후, **별도 서브에이전트**가 적대적 리뷰를 수행한다:

서브에이전트에게 Spec 문서만 전달하고 (구현 맥락 없이) 다음을 검증:
- **완전성**: 모든 엣지케이스가 명세되었는가? 누락된 에러 시나리오는?
- **일관성**: API 계약과 동작 명세가 모순되지 않는가?
- **모호성**: 구현자가 다르게 해석할 수 있는 문장이 있는가?
- **TBD 탐지**: "추후", "나중에", "미정", "TBD" 등 미확정 표현이 있는가?
- **수용 기준 검증 가능성**: 각 수용 기준이 자동화된 테스트로 검증 가능한가?

Spec 리뷰에서 이슈 발견 시 → Spec 수정 → 재리뷰 (통과할 때까지 반복)

### 4단계: 역할 기반 Plan 리뷰
Spec 리뷰 통과 후, 2개의 서로 다른 관점에서 Plan을 리뷰한다.
각 리뷰는 **병렬 서브에이전트**로 실행하며, Spec 문서와 구현 계획을 입력으로 전달한다.

#### 4-A. Product 리뷰 (왜 만드는가?)
다음을 검토:
- 이 기능이 사용자 문제를 실제로 해결하는가?
- 스코프가 적절한가? (과도하거나 부족하지 않은가)
- 놓친 사용자 시나리오가 있는가?
- 에러 상황에서 사용자 경험이 고려되었는가?
- 기존 기능과의 일관성이 유지되는가?

#### 4-B. Engineering 리뷰 (어떻게 만드는가?)
다음을 검토:
- 아키텍처가 기존 시스템과 일관되는가?
- 성능 병목이 예상되는 지점은?
- 보안 고려사항이 충분한가?
- 테스트 전략이 적절한가? (단위/통합/E2E)
- DB 마이그레이션이 무중단으로 가능한가?
- 롤백 시나리오가 고려되었는가?

리뷰 이슈가 있으면 Spec을 자동 수정 → 이슈 해결된 상태로 최종 승인 단계에 진입한다. 중간에 사용자에게 확인하지 않는다.

### 5단계: 최종 승인 (단일 게이트)
분석, Spec, Spec 리뷰, Product/Engineering 리뷰 결과를 **통합한 최종안 1개**를 사용자에게 제시하고 명시적 승인을 받는다.
- 중간 리뷰 결과나 옵션 비교는 요청이 없으면 보고하지 않는다 (사용자 피로 최소화)
- 최종안에 반영된 주요 설계 결정과 근거만 간결히 제시
- **DoD (Definition of Done)** 를 함께 선언한다 — 사용자가 "끝났다"고 동의할 수 있는 **검증 가능한 종료 조건** 1~3개를 명시 (예: "POST /v1/reservations 201 응답 + 신규 통합 테스트 2건 통과", "스키마 마이그레이션 dry-run + rollback 스크립트 검증")
- 사용자 응답("ㅇㅇ", "진행", "좋아" 등) 확인 후에만 구현으로 진행

#### 최종 승인 제시 형식
```
🎯 최종안: [작업명]

근본 원인 / 목적: [요약]
주요 설계 결정: [라이브러리/패턴/API 등 — 근거 1줄씩]
수정 파일: [목록]
위험·롤백: [요약]

✅ DoD (Definition of Done)
  - [검증 가능한 종료 조건 1] — 검증 수단: [테스트/스크립트/로그/스크린샷]
  - [검증 가능한 종료 조건 2] — 검증 수단: [...]

진행할까요?
```

DoD는 9단계 Dashboard·10단계 완료 보고에서 그대로 검증 대상이 된다. "잘 동작함", "기능 추가됨" 같은 검증 불가능한 표현은 DoD가 될 수 없다.

Trivial/Small Track도 구현 착수 전에 **DoD를 한 줄로 선언**한다 (예: "린트·테스트 통과 + 추가된 케이스 1건 통과"). Spec을 작성하지 않더라도 DoD는 면제되지 않는다.

